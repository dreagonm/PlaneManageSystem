//
// Created by 17482 on 2020/12/29.
//
#include <iostream>
#include "test_database.h"
#include "../database/database.h"
#include "../global_config.h"
#ifdef DEBUG
using namespace std;
int main(){


}

/*----------------- TEST_RECORD
    Record TestRecord;
    TestRecord.AddRecordField("a","AD");
    TestRecord.AddRecordField("b","BC");
    TestRecord.AddRecordField("c","DE");
    auto Data=TestRecord.GetAllFields();
    for(auto it=Data.begin();it!=Data.end();it++){
        cout<<(*it).first<<" "<<(*it).second<<endl;
    }
    cout<<TestRecord.GetRecordField("a")<<endl;
    TestRecord.EraseRecordField("a");
    Data=TestRecord.GetAllFields();
    for(auto it=Data.begin();it!=Data.end();it++){
        cout<<(*it).first<<" "<<(*it).second<<endl;
    }
    cout<<TestRecord.CheckRecord("b","BC")<<endl;
    cout<<TestRecord.CheckRecord("b","CD")<<endl;
    set<string> S;
    S.insert("a");
    S.insert("b");
    S.insert("d");
    TestRecord.MaintainRecord(S);
    Data=TestRecord.GetAllFields();
    for(auto it=Data.begin();it!=Data.end();it++){
        cout<<(*it).first<<" "<<(*it).second<<endl;
    }
    ----------*/

#endif

